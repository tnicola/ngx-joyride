export class JoyrideOptions {
    steps: string[];
    startWith?: string;
    stepDefaultPosition?: string;
    themeColor?: string;
    showCounter?: boolean;
    showPrevButton?: boolean;
    logsEnabled?: boolean;
}