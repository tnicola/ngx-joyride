// Only for IDE to find ngx-joyride library
{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "ngx-joyride": [
        "../lib/public_api.ts"
      ]
    }
  },
  "include": [
    "./app/**/*.ts",
    "../lib/**/*.ts"
  ]
}